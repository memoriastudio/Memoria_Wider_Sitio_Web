---
const bottomLinks = [
  {
    links: [
      {
        name: 'Memoria Studio',
        href: 'https://memoriastudio.net/',
        isExternal: true,
      },
      {
        name: 'Instagram',
        href: 'https://www.instagram.com/memoriastudio_llc/',
        isExternal: true,
      },
      {
        name: 'Linkedin',
        href: 'https://www.linkedin.com/company/memoria-studio-llc/',
        isExternal: true,
      },
    ],
  },
];
---

<footer
  class="bg-primary-500/10 py-12 dark:bg-primary-400/10 lg:mt-16 lg:py-16"
  aria-labelledby="footer-heading"
>
  <h2 id="footer-heading" class="sr-only">Footer</h2>
  <div class="mx-auto max-w-2xl px-4 sm:px-6 lg:max-w-7xl lg:px-8">
    <div class="flex flex-col justify-between gap-12 sm:gap-16 lg:flex-row">
      <div class="flex max-w-sm flex-col items-start gap-6">
        <img
          class="aspect-square h-[60px]"
          width="60"
          height="60"
          :src="$store.theme.isDark ? '/assets/logo-light.png' : '/assets/logo-black.png'"
          alt="Stone"
        />
        <p class="text-base text-primary-950/70 dark:text-primary-200/70">
          From the minds at Memoria Studio — discover how we build, scale, and share creative
          systems.
        </p>
        <p class="text-sm text-primary-950/70 dark:text-primary-200/70">© 2025 Memoria Studio</p>
      </div>
      <div class="flex-3 grid max-w-xl grid-cols-3 gap-6 sm:grid-cols-1">
        {
          bottomLinks.map((group) => (
            <div class="flex flex-col gap-4">
              <ul role="list" class="flex flex-col gap-3">
                {group.links.map((link) => (
                  <li>
                    <a
                      href={link.href}
                      class="inline-flex items-center gap-1 rounded-md text-base hover:underline focus-visible:outline focus-visible:outline-2 focus-visible:outline-primary-950 dark:focus-visible:outline-primary-200"
                      target={link.isExternal ? '_blank' : undefined}
                      rel={link.isExternal ? 'noopener' : undefined}
                    >
                      {link.name}
                      {link.isExternal && (
                        <svg
                          class="h-4 w-4"
                          xmlns="http://www.w3.org/2000/svg"
                          viewBox="0 0 24 24"
                          fill="currentColor"
                          aria-hidden="true"
                        >
                          <path d="M10 6V8H5V19H16V14H18V20C18 20.5523 17.5523 21 17 21H4C3.44772 21 3 20.5523 3 20V7C3 6.44772 3.44772 6 4 6H10ZM21 3V11H19L18.9999 6.413L11.2071 14.2071L9.79289 12.7929L17.5849 5H13V3H21Z" />
                        </svg>
                      )}
                    </a>
                  </li>
                ))}
              </ul>
            </div>
          ))
        }
      </div>
    </div>
  </div>
</footer>
